#include<bits/stdc++.h>
using namespace std;
int main(){
	double a,b, product;
	// printf("Enter two numbers: ");
	// scanf("%d",&a);
	scanf("%lf %lf", &a, &b);
	cout<<a<<endl;
	product=a*b;
	printf("Product=%.2If", product);
	return 0;
}